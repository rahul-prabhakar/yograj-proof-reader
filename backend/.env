DATABASE_URL=sqlite:kalash.db
PORT=8080
RUST_LOG=kalash_backend=debug,tower_http=info

SEED_URL=https://proof-reader-0609.herokuapp.com/getAllWords

# Optional: seed words_to_replace from a remote JSON endpoint on first run.
# The endpoint must return an array of { "word": "...", "toBeReplacedWith": "..." }.
# Seeding is skipped automatically once the table has any rows.
# SEED_URL=https://example.com/getAllWords

